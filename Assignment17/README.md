# Promises in JS
- This program illustrates the use of promises in JS
- Promises take two arguments resolve and reject
- If the code runs without any error the promise resolves with the given resolve value, however; any errors in the runtime rejects the promise with reject message in console
- This program checks if user is trying to divide a number by 0 , it rejectes in the case if so
